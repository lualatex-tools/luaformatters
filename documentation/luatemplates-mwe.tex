\documentclass{article}

\usepackage[color]{luatemplates}
\addLuaTemplates{luatemplates-mwe-templates.lua}

\begin{document}
\section*{\package{luatemplates} -- \textsc{mwe}}

\myBold{This} is a MWE for setting up a document with \package{luatemplates}
support.  The commands \cmd{package} and \cmd{cmd} have been set up using
\emph{style} templates, while \cmd{XXX} (\XXX{Foo}) was defined as a simple
formatter function.

\end{document}
